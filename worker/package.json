{
  "name": "toll-api-worker",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "types": "wrangler types",
    "typecheck": "wrangler types && tsc --noEmit -p tsconfig.json",
    "test": "node --experimental-strip-types --disable-warning=MODULE_TYPELESS_PACKAGE_JSON --test tests/http.test.ts"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "wrangler": "^4.131.1"
  }
}
