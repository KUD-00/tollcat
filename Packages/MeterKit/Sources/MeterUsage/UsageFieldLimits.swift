// GENERATED — 由 scripts/generate-shared.py 从 shared/api-contract.json 生成。
// 不要手改：改 shared/api-contract.json 后重跑生成器。

import Foundation

/// 和 Worker 的常量同一份（shared/api-contract.json）。
public enum UsageFieldLimits: Sendable {
    public static let version = 40
    public static let screens = 32
    public static let count = 1000
}
